fix the errors in https://github.com/yennster/fanficly/pull/21, and validate https://github.com/yennster/fanficly/pull/20 to see if it is a good fix or not

also add a feature togggle in settings to toggle rendering of images included in stores or not, currently no images are rendered and leave that as the default

also spawn agents to audit and verify code, find and fix bugs, etc.

---

<task-notification>
<task-id>bwuc1qnee</task-id>
<summary>Monitor event: "PR #20 CI (Build &amp; Test) final result after 429 fix push"</summary>
<event>Build &amp; Test	pass	16m6s	https://github.com/yennster/fanficly/actions/runs/29041781734/job/86200493649</event>
If this event is something the user would act on now, send a PushNotification. Routine or benign output doesn't need one.
</task-notification>

---

<task-notification>
<task-id>w0w1cp8a4</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-jenny-Work-ao3-app/936f110b-7dd0-4618-ae44-7b95062ac703/tasks/w0w1cp8a4.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Review the image-toggle diff + PR21 fix, hunt real bugs across the repo, adversarially verify" completed</summary>
<result>{"confirmed":[{"title":"Continuous/swipe reading-position restore drifts as images load (no reserved height)","severity":"major","detail":"In continuous and swipe-by-chapter modes, ReaderInlineImage renders with no fixed height (Fanficly/Reader/ChapterContentView.swift:109-143): the AsyncImage .empty phase is a ~20pt ProgressView that grows to up to 480pt on success. restoreContinuous (Fanficly/Reader/ReaderView.swift:634-648) and restorePaginatedParagraph (:938-951) retry proxy.scrollTo for only ~1.8s. Failure scenario: images enabled, reopen a work with e.g. 5 embedded images above the saved paragraph in the current chapter; on a cold cache the author's image host (imgur/Discord CDN etc.) responds after the retry window, each image grows by up to ~460pt, pushing the restored paragraph far off-screen — the user reliably loses their place every reopen of an image-heavy fic. The same late reflow also throws off the karaoke auto-scroll target position mid-narration. (Paginated page-by-page mode is immune because it uses fixedHeight 240.)","file":"Fanficly/Reader/ChapterContentView.swift","suggestedFix":"Reserve layout space for unloaded images in the flowing modes: give ReaderInlineImage a fixed placeholder height (e.g. the same 240pt used by pagination, or a stored aspect ratio) for the .empty/.failure phases so content below an image never shifts when it loads; optionally relax to natural height only after success if the extra reflow is acceptable post-restore.","source":"diff:alignment","votes":[{"isReal":true,"reasoning":"Confirmed against the working tree. In continuous and swipe-by-chapter modes, ReaderInlineImage (Fanficly/Reader/ChapterContentView.swift:109-143) is used with fixedHeight nil: the .empty phase is a bare ProgressView (~20pt, no reserved height — .frame(maxHeight: 480) is only a cap), and .success renders an aspect-fit image up to 480pt, so each late-loading image grows content above the viewport b"},{"isReal":true,"reasoning":"Confirmed against the working tree. (1) In continuous and swipe-by-chapter modes, ChapterContentView renders ReaderInlineImage with fixedHeight=nil (Fanficly/Reader/ChapterContentView.swift:48,114-140): the .empty phase is a bare ProgressView (~20pt) and .frame(maxHeight: 480) only caps, never reserves, so a successful load grows the slot by up to ~460pt; only ReaderPageCell passes fixedHeight 240"},{"isReal":true,"reasoning":"Could not refute. All mechanical claims verified in the working tree: (1) ChapterContentView.swift:48 renders ReaderInlineImage with fixedHeight=nil in continuous/swipe modes, and the frame chain (:138-140) uses only maxHeight:480 — a ceiling, not a floor — so the .empty ProgressView measures ~20pt and grows to up to ~480pt on success; (2) ChapterContentView is a plain non-lazy VStack, so every As"}],"confirmed":true},{"title":"Continuous-mode image slots collapse to spinner height, then reflow on load — breaks position restore and can regress saved progress","severity":"major","detail":"ReaderInlineImage with fixedHeight == nil (continuous and swipe-by-chapter modes, Fanficly/Reader/ChapterContentView.swift:120-142) reserves no height while AsyncImage is in .empty: the ProgressView slot is ~20pt, and on load the image grows to up to 480pt (probe-verified: a tall image lays out at 480pt). ChapterContentView's VStack is not lazy, so every image in a chapter starts loading the moment the chapter renders and each one that resolves pushes all following text down by up to ~460pt. Failure scenario: user enables Show Images, reopens an image-heavy work saved at paragraph 200. restoreContinuous (Fanficly/Reader/ReaderView.swift:634-648) retries scrollTo for only ~2.05s and then sets isRestoring = false; on a slow connection images above paragraph 200 finish after that window, shoving the restored paragraph down/off screen. Worse, anchor sampling has resumed (ReaderView.swift:581-590), so the now-topmost earlier paragraph is written back via saveProgress (ReaderView.swift:612-614) — the saved position silently regresses without the user scrolling. Same ~1.8s window applies to restorePaginatedParagraph (ReaderView.swift:938-951).","file":"Fanficly/Reader/ChapterContentView.swift","suggestedFix":"Reserve a stable height for the un-loaded phases: give .empty and .failure the same footprint the loaded image will get (e.g. .frame(height: fixedHeight ?? ReaderInlineImage.paginatedHeight) for the placeholder phases, or a fixed 240pt slot in continuous mode too). That makes load completion layout-neutral, so restore and anchor-based progress saving are unaffected.","source":"diff:rendering","votes":[{"isReal":true,"reasoning":"Every step of the claimed chain checks out against the working tree. (1) ReaderInlineImage with fixedHeight == nil (continuous and swipe-by-chapter) renders the .empty phase as a bare ProgressView under .frame(maxWidth:.infinity).frame(maxHeight:480) — maxHeight caps but does not reserve, so the slot is ~20pt while loading and grows to up to 480pt on success, shifting all following content by up t"},{"isReal":true,"reasoning":"Could not refute; every step verified in code. (1) ReaderInlineImage with fixedHeight == nil (continuous + swipe-by-chapter, ChapterContentView.swift:48, 120-142) renders .empty as a bare ProgressView under .frame(maxWidth:.infinity).frame(maxHeight:480) — no minimum height, so the slot is ~20pt and grows to up to 480pt on .success; load is not layout-neutral. (2) ChapterContentView's paragraph co"},{"isReal":true,"reasoning":"Could not refute it. NOT preexisting: before imagetoggle.diff, &lt;img&gt; tags were dropped entirely (includeImages:false is documented as \"the historical behavior\"), so image-load reflow only exists because of this diff. NOT hypothetical — every link verified in the working tree: (1) ReaderInlineImage (ChapterContentView.swift:120-142) reserves no height in .empty — bare ProgressView (~20pt); .frame(m"}],"confirmed":true},{"title":"Settings footer claims \"Images are never stored\" but AsyncImage persists them to the shared disk URLCache","severity":"major","detail":"ReaderSettingsView.swift:~255 footer says \"Images are never stored, so downloaded works stay text-only offline.\" But ReaderInlineImage (ChapterContentView.swift:106-143) uses AsyncImage, which loads via URLSession.shared and the default shared URLCache — the code comment at ChapterContentView.swift:107 even relies on it (\"the shared URLCache gives basic reuse\"). The default shared URLCache writes responses to disk (Library/Caches/&lt;bundle&gt;/Cache.db); no custom/ephemeral URLCache is configured anywhere in the app (verified by grep). Concrete scenario: a user enables the toggle, reads an explicit-rated fic with embedded images, and trusts the copy that nothing is stored — yet the images persist on disk in the app's cache (surviving app restarts, included in unencrypted local backups, discoverable forensically). Corollary: a \"downloaded work\" opened offline can still display recently-viewed images from that cache, contradicting \"stay text-only offline\". For an app whose headline feature is its privacy posture, a false privacy claim in settings copy is a real defect. Related nit: third-party image hosts can also set cookies into HTTPCookieStorage.shared, which are persisted and re-sent — slightly beyond the footer's \"it can see your IP address\" framing.","file":"Fanficly/Settings/ReaderSettingsView.swift","suggestedFix":"Either make the copy honest (e.g. \"Images are not saved with downloaded works; the system may keep a temporary cache of recently viewed images on this device\") or make the claim true: load images through a dedicated URLSession with an ephemeral/memory-only URLCache (requires replacing AsyncImage with a small custom loader) and a cookie-less configuration.","source":"diff:privacy-ux","votes":[{"isReal":true,"reasoning":"Verified in the working tree: ReaderSettingsView.swift:258 contains the exact footer copy \"Images are never stored, so
... (truncated 86123 chars, full result in /private/tmp/claude-501/-Users-jenny-Work-ao3-app/936f110b-7dd0-4618-ae44-7b95062ac703/tasks/w0w1cp8a4.output)</result>
<diagnostics>Per-agent results: /Users/jenny/.claude/projects/-Users-jenny-Work-ao3-app/936f110b-7dd0-4618-ae44-7b95062ac703/subagents/workflows/wf_58ec7a24-352/journal.jsonl — one {"type":"result",...} line per completed agent with its full return value.
If the result above is empty or unexpected, Read this file BEFORE diagnosing — do not assume agents returned non-empty results.
To re-run with edited post-processing: Workflow({scriptPath: '/Users/jenny/.claude/projects/-Users-jenny-Work-ao3-app/936f110b-7dd0-4618-ae44-7b95062ac703/workflows/scripts/audit-and-bughunt-wf_58ec7a24-352.js', resumeFromRunId: 'wf_58ec7a24-352'}) — agents whose (prompt, opts) are unchanged replay from cache.</diagnostics>
<usage><agent_count>61</agent_count><agents_done>61</agents_done><agents_error>0</agents_error><agents_skipped>0</agents_skipped><agents_empty_result>0</agents_empty_result><subagent_tokens>4794119</subagent_tokens><tool_uses>721</tool_uses><duration_ms>1519379</duration_ms></usage>
</task-notification>

---

<system-reminder>
The user started your suggested background task task_a9fba636 ("Fix iCloud backup data-loss bugs (LWW shrink, chapter wipe)") in a separate local session. It is running independently. You will be notified here when it ends.
</system-reminder>
<system-reminder>
The user started your suggested background task task_d3c46d80 ("Fix subscription sync & notification reliability") in a separate local session. It is running independently. You will be notified here when it ends.
</system-reminder>
<system-reminder>
The user started your suggested background task task_f90a192f ("Fix reading-stats accuracy (phantom time, Finished threshold)") in a separate local session. It is running independently. You will be notified here when it ends.
</system-reminder>
<system-reminder>
The user started your suggested background task task_14ec13aa ("Fix Popular tab live facets (AO3 markup mismatch)") in a separate local session. It is running independently. You will be notified here when it ends.
</system-reminder>
<system-reminder>
The user started your suggested background task task_8ef1982b ("Stop stale widget taps regressing reading position") in a separate local session. It is running independently. You will be notified here when it ends.
</system-reminder>
<system-reminder>
The user started your suggested background task task_36598192 ("Make reader-profile deletion survive iCloud KVS merge") in a separate local session. It is running independently. You will be notified here when it ends.
</system-reminder>

you do it