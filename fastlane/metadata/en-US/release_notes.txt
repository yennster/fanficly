Popular is now live. The fandoms, ships, and characters lists are ranked from AO3's own work counts and refresh automatically, instead of a fixed curated list. When you're offline or AO3 is unreachable, the tab falls back to the curated picks, so it's never empty.
